# Base URL of the Stashly backend API
NUXT_PUBLIC_API_BASE=http://127.0.0.1:3000

# Mark the auth cookie Secure. Set true only when served behind HTTPS,
# otherwise browsers will drop the cookie entirely.
NUXT_PUBLIC_COOKIE_SECURE=false
